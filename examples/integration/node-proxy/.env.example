FASTAPI_URL=http://localhost:8000
# Option 1: Provide a ready API token (Authorization: Bearer <token>)
# API_TOKEN=

# Option 2: Or let the proxy obtain a token from FastAPI auth with these credentials
API_EMAIL=demo@example.com
API_PASSWORD=demopass123

# CORS origins allowed for the proxy (comma separated)
PROXY_CORS_ORIGINS=http://localhost:3000,http://localhost:5173
